import React from 'react';

const HomePage = () => (
  <div style={{
    display: 'flex',
    alignItems: 'center',
    justifyContent: 'center',
    height: '60vh',
    fontWeight: 700,
    fontSize: 48
  }}>
    Welcome to chAInSight!
  </div>
);

export default HomePage;
